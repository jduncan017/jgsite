import { Cardo, Enriqueta, Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
});
export const enriqueta = Enriqueta({
  weight: ["400", "700"],
  subsets: ["latin"],
});
export const cardo = Cardo({
  weight: ["400", "700"],
  subsets: ["latin"],
});
